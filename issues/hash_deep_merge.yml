---
level: easy
tags: [hash]
description:
  Returns a new hash with self and other_hash merged recursively.

checks:
  ruby:
    setup: |
      h1 = { x: { y: [4, 5, 6] }, z: [7, 8, 9] }
      h2 = { x: { y: [7, 8, 9] }, z: 'xyz' }

    asserts:
      - assert_equal({:x => {:y => [7, 8, 9]}, :z => "xyz"}, h1.deep_merge(h2))
      - assert_equal({:x => {:y => [4, 5, 6]}, :z => [7, 8, 9]}, h2.deep_merge(h1))
      - assert_equal({:x => {:y => [4, 5, 6, 7, 8, 9]}, :z => [7, 8, 9, 'xyz']}, h1.deep_merge(h2) { |key, old, new| Array.wrap(old) + Array.wrap(new) })

author:
  github_nickname: zzet
  web_page: "http://zzet.org"
