---
level: medium
tags: []
description: "Write some code to sum the first 1000 primes without using Ruby's Prime class."

checks:
  ruby:
    asserts:
      - assert_equal 3682913, sum_primes(1000)
      - assert_equal 111587, sum_primes(200)

  javascript:
    asserts:
      - assertEqual(3682913, sumPrimes(1000))
      - assertEqual(111587, sumPrimes(200))
