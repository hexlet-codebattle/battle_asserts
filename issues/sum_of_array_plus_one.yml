level: elementary
tags: [array, arithmetic]
description: |
  Write a function that takes an array of integers
  and returns the sum of the integers after adding 1 to each.
checks:
  javascript:
    asserts: ['assertEqual(14, plusOneSum([1, 2, 3, 4]))', 'assertEqual(16, plusOneSum([12,
        -3, 0, 3]))']
