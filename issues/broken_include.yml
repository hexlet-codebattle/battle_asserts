---
level: elementary
tags: [enumerable, set]
description: |
  Method .include? in Array (Enumerable) not work. Check presence of element in array.

checks:
  ruby:
    setup: >
      set = [1, 2, 3, 4, 5, 6, 7]

    asserts:
      - assert include?(set, 5)
      - assert !include?(set, 10)
