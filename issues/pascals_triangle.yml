checks:
  php: |-
    assertEqual(array(1), pt(0));
    assertEqual(array(1, 1), pt(1));
    assertEqual(array(1, 2, 1), pt(2));
    assertEqual(array(1, 3, 3, 1), pt(3));
    assertEqual(array(1, 4, 6, 4, 1), pt(4));
  python: |-
    assert_equal([1], pt(0))
    assert_equal([1, 1], pt(1))
    assert_equal([1, 2, 1], pt(2))
    assert_equal([1, 3, 3, 1], pt(3))
    assert_equal([1, 4, 6, 4, 1], pt(4))
  javascript: |-
    assertEqual([1], pt(0));
    assertEqual([1, 1], pt(1));
    assertEqual([1, 2, 1], pt(2));
    assertEqual([1, 3, 3, 1], pt(3));
    assertEqual([1, 4, 6, 4, 1], pt(4));
  ruby: |-
    assert_equal([1], pt(0))
    assert_equal([1, 1], pt(1))
    assert_equal([1, 2, 1], pt(2))
    assert_equal([1, 3, 3, 1], pt(3))
    assert_equal([1, 4, 6, 4, 1], pt(4))
level: medium
tags: [arithmetic, math]
description: Write a method that will return the nth row of Pascal's Triangle.
